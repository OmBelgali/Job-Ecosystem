// Rule-based fallback responses when AI providers are unavailable.

export function ruleBasedFallback(input: string): string {
  return "AI response is temporarily unavailable. Please try again later.";
}
